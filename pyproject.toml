[tool.poetry]
name = "new-website"
version = "0.1.0"
description = "Minimal Quarto website migration workspace"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
pyyaml = "^6.0.2"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
